package homework12;

public class Track extends Vehicle{

    public Track(String vehicleType, int plate, int speed, int weight, double width, int height, int length){
        super(vehicleType, plate, speed, weight, width, height, length);
    }
}